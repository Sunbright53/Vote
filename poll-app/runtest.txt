// 🔒 ถ้าเครื่องนี้โหวตไปแล้วสำหรับ batch นี้ → เด้งไป /done
        // if (localStorage.getItem(votedKey(b))) {
        //   nav('/done?already=1', { replace: true });
        //   return;
        // }




cd poll-app/src
rm -f main.js App.js api.js types.js
cd ..
rm -rf ../docs dist
npm run build
cp dist/index.html dist/404.html
touch dist/.nojekyll
mv dist ../docs
cd ..
git add -A
git commit -m "hide button, when we finish vote"
git push



cd "/Users/ciy_th/Desktop/Poll-Vote/poll-app"
npm install
npm run dev